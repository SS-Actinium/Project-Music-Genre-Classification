# Core (features, CLI shell, tests — works without TF)
numpy>=1.24,<3
scipy>=1.10
librosa>=0.10,<0.12
soundfile>=0.12
scikit-learn>=1.3

# API
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# Inference / train (Python 3.13 needs TF 2.20+)
# Install explicitly: pip install "tensorflow>=2.20,<2.23"
tensorflow>=2.20,<2.23

# Dev / test
pytest>=8.0
